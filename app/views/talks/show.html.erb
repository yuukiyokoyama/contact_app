<% title = @talk.title(current_user, 50)%>
<% provide(:totle, title)%>
<h1><%=title %></h1>

<div class="row">
 <div class="col=md-8 col-md-offset-2">
  <ul class="messages">
   <%= render @messages %>
   </ul>

   <%= form_for @message, url: messages_talk_path(@talk),html: { class: 'message=from'} do |f|%>
   <%= render 'shared/error_messages', object: f.object %>
   <div class="field">
   <%= f.text_sres :content, class: "content"%>
   </div>
   <%= f.dubmit "Send", class: "btn btn-primary" %>
   <% end %>
   </div>
   </div>